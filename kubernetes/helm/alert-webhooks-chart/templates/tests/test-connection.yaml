apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "alert-webhooks-chart.fullname" . }}-test-connection"
  labels:
    {{- include "alert-webhooks-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "alert-webhooks-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
