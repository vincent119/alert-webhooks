apiVersion: v1
kind: Service
metadata:
  name: {{ include "alert-webhooks-chart.fullname" . }}
  labels:
    {{- include "alert-webhooks-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "alert-webhooks-chart.selectorLabels" . | nindent 4 }}
